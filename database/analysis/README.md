# Database analysis

Place reproducible exploratory and publication analysis scripts here.

Scripts should read from `database/source/variants.csv` or the generated
`Scores_FECH_UROD.csv`, state their required packages, and write derived
outputs to a clearly named subfolder. Files in this folder are not deployed
with the Shiny application.
